import { IfasDirective } from './ifas.directive';

describe('IfasDirective', () => {
  it('should create an instance', () => {
    // const directive = new IfasDirective();
    // expect(directive).toBeTruthy();
  });
});
